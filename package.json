{
  "name": "drdurairajarjunan-site",
  "version": "0.1.0",
  "private": true,
  "description": "Static doctor profile website for drdurairajarjunan.com.",
  "scripts": {
    "build": "rm -rf _site && mkdir _site && cp index.html CNAME robots.txt sitemap.xml _site/ && cp -R assets _site/assets",
    "start": "vite --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^8.1.3"
  }
}
